# セルフホスト計画

> 最終目標である「Mython で書いた Mython コンパイラが自分自身をコンパイルできる」状態への
> 道筋と、達成を検証する方法を定義します。
>
> **第1章の時点でこれを読む必要はありません**が、
> 各章で「なぜこの機能を入れるのか」の答えがここにあります。

---

## 1. セルフホストとは何か

```
【今（第1章〜第15章）】
  mythonc (C で書かれた実行ファイル)  +  foo.my  →  foo (実行ファイル)

【目標（第20章）】
  mythonc2 (Mython で書かれた実行ファイル)  +  selfhost/*.my  →  mythonc2 と同じもの
```

コンパイラが**自分自身のソースコードをコンパイルできる**状態です。

### 🤔 なぜこれを目標にするのか

1. **言語が実用に足ることの最も厳しい証明**
   コンパイラは、文字列処理・データ構造・再帰・ファイル入出力・大規模なコード整理を
   すべて必要とします。それが書けるなら、その言語は実用言語です。

2. **バグを自分で踏むことになる**
   自分の言語で数千行を書くと、設計の不便な点が容赦なく明らかになります。
   「型注釈が冗長すぎる」「エラーメッセージが不親切」を、**自分が最初のユーザーとして体験**します。

3. **鶏と卵の問題を解く経験**
   「コンパイラを書くにはコンパイラが必要」というパラドックスを、
   ブートストラップという手法で解きます。これは計算機科学の古典的な美しさの 1 つです。

---

## 2. ブートストラップの仕組み

「鶏と卵」は、**すでに存在する別のコンパイラを踏み台にする**ことで解けます。

```
                     ┌─────────────────────────────────────┐
                     │  ソースコード（変わらない）            │
                     │  selfhost/*.my                      │
                     └─────────────────────────────────────┘
                                    │
   ┌────────────────────────────────┼────────────────────────────────┐
   │                                │                                │
   ▼                                ▼                                ▼
┌──────────┐  compiles    ┌──────────┐  compiles    ┌──────────┐  compiles  ┌──────────┐
│ stage0   │─────────────▶│ stage1   │─────────────▶│ stage2   │───────────▶│ stage3   │
│ (C 製)   │              │(Mython製)│              │(Mython製)│            │(Mython製)│
└──────────┘              └──────────┘              └──────────┘            └──────────┘
   ↑                                                     │                       │
   │                                                     └────── 比較 ───────────┘
第1〜15章で作る                                                     │
                                                            一致 ⇒ 成功 🎉
```

### 各 stage の意味

| stage | 何で書かれているか | 何でコンパイルされたか |
|---|---|---|
| **stage0** | C 言語 | Apple clang（外部のコンパイラ） |
| **stage1** | Mython | stage0 |
| **stage2** | Mython | stage1 |
| **stage3** | Mython | stage2 |

### 🤔 なぜ stage1 と stage2 は一致しなくてよいのか

**ここがブートストラップで最も混乱しやすい点です。**

stage1 と stage2 は、**同じソースから作られた**のに一致しないことがあります。

```
stage1 = stage0(selfhost/*.my)      ← C 製コンパイラの出力
stage2 = stage1(selfhost/*.my)      ← Mython 製コンパイラの出力
```

理由：**stage0 と stage1 は「別のコンパイラ」だから**です。
両者は同じ言語仕様を実装していますが、実装が違います。たとえば：

- stage0（C 版）は一時レジスタ名を `%t0, %t1, ...` と振る
- stage1（Mython 版）はたまたま評価順が違い、`%t0, %t2, %t1` と振る

どちらも**意味は同じで正しい**IR ですが、バイト列としては違います。
だから stage1 ≠ stage2 でも問題ありません。

一方、

```
stage2 = stage1(selfhost/*.my)
stage3 = stage2(selfhost/*.my)
```

**stage2 と stage3 は一致しなければなりません。**
なぜなら stage1 と stage2 は「同じソースを同じ仕様で実装した Mython 製コンパイラ」であり、
**stage2 が正しく stage1 と同じ振る舞いをするなら、その出力も同じ**になるはずです。

これを **不動点（fixed point）に到達した**と言います。

```
f = 「selfhost/*.my をコンパイルする」という関数
stage2 = f(stage1)
stage3 = f(stage2)
stage2 == stage3  ⇔  stage2 は f の不動点  ⇔  セルフホスト成功
```

### ⚠️ もし stage2 ≠ stage3 だったら

それは **stage1 にバグがある**という意味です。具体的には：

- stage1 が生成した stage2 が、stage1 と違う振る舞いをする
- つまり stage1 のコード生成に「自分自身を正しく再現できない」欠陥がある

デバッグ手順は第20章で詳しく扱いますが、基本は
「stage1 と stage2 に同じ小さな入力を与えて、出力 IR を diff する」です。

---

## 3. セルフホストに必要な言語機能のチェックリスト

**このチェックリストが第15章の到達目標です。**
すべて ✅ になれば、Mython でコンパイラが書けます。

### 3.1 基本

| 機能 | 必要な理由 | 章 | 状態 |
|---|---|---|---|
| `int` 型と算術演算 | カウンタ、オフセット計算 | ch1-2 | ⬜ |
| `bool` と比較・論理演算 | 条件判定のすべて | ch6 | ⬜ |
| `if` / `elif` / `else` | 分岐（トークン種別の判定など） | ch7 | ⬜ |
| `while` / `break` / `continue` | 字句解析のループ | ch7 | ⬜ |
| 関数定義・呼び出し | コードの整理 | ch8 | ⬜ |
| **再帰呼び出し** | **再帰下降パーサに必須** | ch8 | ⬜ |
| 全経路 return 検査 | バグ防止（必須ではないがあると助かる） | ch8 | ⬜ |

### 3.2 文字列（コンパイラの主戦場）

| 機能 | 必要な理由 | 章 | 状態 |
|---|---|---|---|
| `str` 型とリテラル | ソースコードそのもの、IR の出力 | ch9 | ⬜ |
| `len(s)` | 走査の終了判定 | ch9 | ⬜ |
| `s[i]`（1 文字取得） | 字句解析の 1 文字読み | ch9 | ⬜ |
| `a + b`（連結） | IR 文字列の組み立て | ch9 | ⬜ |
| `a == b`（内容比較） | キーワード判定 | ch9 | ⬜ |
| `str(n)`（int → str） | レジスタ番号 `%t42` の生成 | ch9 | ⬜ |
| `int(s)`（str → int） | 整数リテラルのパース | ch9 | ⬜ |
| `ord(c)` / `chr(n)` | 文字の分類（数字か英字か） | ch9 | ⬜ |
| 部分文字列 `substr(s,i,n)` | トークンのテキスト切り出し | ch9 | ⬜ |
| エスケープ処理 | 文字列リテラルの扱い | ch9 | ⬜ |

**⚠️ 文字列機能が最も重要です。** コンパイラは本質的に文字列処理プログラムです。
第9章を手を抜くと、第16章以降で必ず戻ってくることになります。

### 3.3 データ構造

| 機能 | 必要な理由 | 章 | 状態 |
|---|---|---|---|
| `list[T]` の生成・`append` | トークン列、引数リスト、文のリスト | ch10 | ⬜ |
| `xs[i]` の読み書き | 任意位置アクセス（パーサの先読み） | ch10 | ⬜ |
| `len(xs)` | 走査 | ch10 | ⬜ |
| `for x in xs` | 走査 | ch11 | ⬜ |
| `for i in range(n)` | インデックス走査 | ch11 | ⬜ |
| `class` とフィールド | **Token / Node / Type の表現** | ch12 | ⬜ |
| メソッド | 整理（`tok.is_keyword()` など） | ch12 | ⬜ |
| **`T \| None`** | **AST の子ノードが無い場合（`else` 節なし等）** | ch15 | ⬜ |
| `list[Node]`（参照型のリスト） | 文のリスト、引数リスト | ch10+12 | ⬜ |
| `dict[str, X]`（あるいは線形探索の代替） | シンボルテーブル | ch14 | ⬜ |

**⚠️ `T | None` は必須です。** AST のノードは
「`else` 節がある/ない」「型注釈が解決済み/未解決」のような
**「値が無い」状態を必ず持ちます**。これが表現できないと AST が書けません。

**🤔 `dict` は本当に必要か**
シンボルテーブルは線形探索（`list` を舐める）でも動きます。
第14章で `dict` を作りますが、**間に合わなければ線形探索で先に進んでよい**です。
コンパイラの速度は第20章の後に測って直します。

### 3.4 入出力

| 機能 | 必要な理由 | 章 | 状態 |
|---|---|---|---|
| `print(s)` | IR の出力、デバッグ | ch7,9 | ⬜ |
| ファイル全体の読み込み | ソースコードを読む | ch14 | ⬜ |
| ファイルへの書き込み | `.ll` を出力する | ch14 | ⬜ |
| コマンドライン引数の取得 | `mythonc foo.my -o foo` | ch14 | ⬜ |
| 外部コマンド実行（`system`） | `clang foo.ll -o foo` を呼ぶ | ch14 | ⬜ |
| 終了コード指定（`exit(n)`） | エラー時の終了 | ch9 | ⬜ |
| stderr への出力 | エラーメッセージ | ch9 | ⬜ |

**⚠️ 見落としやすい項目**：`argv` の取得と `system()` 呼び出しです。
これがないと、Mython 製コンパイラは「コマンドラインツール」になれません。
第14章で必ず入れます。

### 3.5 プログラムの整理

| 機能 | 必要な理由 | 章 | 状態 |
|---|---|---|---|
| `import`（複数ファイル） | 4000 行を 1 ファイルに書きたくない | ch13 | ⬜ |
| `extern def` | C ランタイムを呼ぶ | ch9 | ⬜ |
| グローバル変数 | 現在位置などのコンパイラ状態 | ch5 | ⬜ |

### 3.6 意図的に「不要」と判断した機能

セルフホストに**要らない**ものを明記します。これを作らない判断が完成を早めます。

| 機能 | なぜ不要か |
|---|---|
| クロージャ / 第一級関数 | 再帰下降パーサは関数の直接呼び出しで書ける |
| 継承 / インターフェース | `Node` は「全部入り構造体」方式（[architecture.md](architecture.md) 3.2）で表現する |
| 例外 | エラーは `panic()` で即終了する方針 |
| ジェネリクス（ユーザー定義） | `list[T]` の組み込み対応で足りる |
| 演算子オーバーロード | 使わない |
| タプル / 複数戻り値 | class を返せば済む |
| イテレータプロトコル | `for` は `list` と `range` に限定 |
| 文字列フォーマット（f-string） | `"a" + str(n) + "b"` で書ける（冗長だが動く） |
| GC | [memory-model.md](memory-model.md) 参照 |
| モジュールの循環 import | ファイル構成を DAG に保つ |

**🤔 f-string について**
`"%s = add i64 %s, %s"` 相当が書けないと IR 生成が非常に冗長になります。
しかし `+` と `str()` の組み合わせで書けるので、**言語機能としては不要**です。
代わりに第14章で**ヘルパ関数**を用意します。

```python
# lib/strutil.my
def cat3(a: str, b: str, c: str) -> str:
    return a + b + c

def cat5(a: str, b: str, c: str, d: str, e: str) -> str:
    return a + b + c + d + e
```

これで IR 生成がこう書けます。

```python
emit(cat5("  ", t, " = add i64 ", l, ", " + r))
```

美しくはありませんが、**言語機能を増やさずに問題を解決**しています。
この種のトレードオフ判断が、言語設計の実践です。

---

## 4. 移植の順序と検証方法

**各段階で C 版が「正解」を持っている**ことを最大限に活用します。

### 第16章：lexer.my

```bash
# 同じ入力に対するトークン列を比較する
./build/mythonc      --dump-tokens tests/cases/all_syntax.my > /tmp/c.txt
./build/stage1-lexer --dump-tokens tests/cases/all_syntax.my > /tmp/m.txt
diff /tmp/c.txt /tmp/m.txt && echo "✅ 一致"
```

**★ `--dump-tokens` を第1章で作っておく理由がここにあります。**
これがあるから、字句解析器だけを独立して検証できます。

### 第17章：parser.my

```bash
./build/mythonc       --dump-ast tests/cases/all_syntax.my > /tmp/c.txt
./build/stage1-parser --dump-ast tests/cases/all_syntax.my > /tmp/m.txt
diff /tmp/c.txt /tmp/m.txt && echo "✅ 一致"
```

AST を **S 式**で出力するのは、テキスト比較できるようにするためです。

```
(func main () int
  (block
    (return (binop + (int 1) (binop * (int 2) (int 3))))))
```

### 第18章：sema.my

型検査は「通る/通らない」と「エラーメッセージ」が出力です。

```bash
# 型エラーのテストケースすべてで、両者が同じエラーを出すか
for f in tests/cases/err_*.my; do
    a=$(./build/mythonc      "$f" 2>&1 >/dev/null)
    b=$(./build/stage1-sema  "$f" 2>&1 >/dev/null)
    [ "$a" = "$b" ] || echo "❌ $f"
done
```

### 第19章：codegen.my

```bash
./build/mythonc        -S tests/cases/all_syntax.my -o /tmp/c.ll
./build/stage1-codegen -S tests/cases/all_syntax.my -o /tmp/m.ll
diff /tmp/c.ll /tmp/m.ll && echo "✅ IR 完全一致"
```

**⚠️ ここで完全一致を目指すべきか？**

目指すべきです。理由：**一致していれば、それ以上の検証が不要**になります。
「意味は同じだが表記が違う」を許すと、比較のたびに人間が判断する必要が生じます。

そのために、C 版と Mython 版で**同じアルゴリズム・同じ順序**で実装します。
機械的な移植（1 行ずつ書き写す）が最も安全です。**ここで独創性を発揮しないこと。**

### 第20章：ブートストラップ

```bash
make bootstrap
```

```makefile
# Makefile（第20章で追加）
SELF = selfhost/main.my

bootstrap: build/mythonc
	@echo "── stage1: C 製コンパイラで Mython 製コンパイラをビルド ──"
	./build/mythonc $(SELF) -o build/stage1

	@echo "── stage2: stage1 で自分自身をビルド ──"
	./build/stage1 $(SELF) -o build/stage2

	@echo "── stage3: stage2 で自分自身をビルド ──"
	./build/stage2 $(SELF) -o build/stage3

	@echo "── 不動点の検証 ──"
	@if cmp -s build/stage2 build/stage3; then \
		echo "✅ stage2 == stage3 : セルフホスト達成！"; \
	else \
		echo "❌ stage2 != stage3 : stage1 にバグがあります"; \
		exit 1; \
	fi
```

**⚠️ バイナリ比較の前に IR で比較する**ほうがデバッグしやすいです。

```bash
./build/stage1 -S $(SELF) -o /tmp/s2.ll
./build/stage2 -S $(SELF) -o /tmp/s3.ll
diff /tmp/s2.ll /tmp/s3.ll        # 差分が見える形で出る
```

バイナリの `cmp` は「違う」しか教えてくれません。**IR の diff なら原因が読めます。**

---

## 5. selfhost/ のファイル構成

C 版と 1:1 に対応させます。

| C 版 | Mython 版 | 内容 |
|---|---|---|
| `src/main.c` | `selfhost/main.my` | 引数処理、パス起動 |
| `src/util.c` | `lib/strutil.my` ほか | 文字列ヘルパ（標準ライブラリへ） |
| `src/lexer.c` | `selfhost/lexer.my` | 字句解析 |
| `src/ast.c` | `selfhost/ast.my` | Node クラス定義とダンプ |
| `src/types.c` | `selfhost/types.my` | Type クラス |
| `src/parser.c` | `selfhost/parser.my` | 構文解析 |
| `src/sema.c` | `selfhost/sema.my` | 型検査 |
| `src/codegen.c` | `selfhost/codegen.my` | IR 出力 |
| `src/diag.c` | `selfhost/diag.my` | エラー報告 |

**この 1:1 対応を崩さないこと。** 崩すと「C 版のどこを見れば正解がわかるか」が失われます。

---

## 6. よくある失敗と対策

| 失敗 | 症状 | 対策 |
|---|---|---|
| 文字列機能が足りない | 第16章で行き詰まる | 第9章で 3.2 のチェックリストを全部埋める |
| `T \| None` を後回しにした | AST が定義できない | 第15章で必ず実装する（第12章から移動。理由は ch12 12.1 節） |
| C 版と違うアルゴリズムで書いた | 出力が一致せず原因が追えない | **機械的に移植する。工夫しない** |
| 一気に全部移植した | どこが壊れたかわからない | ファイル単位で、C 版と差分検証しながら進める |
| `argv` / `system()` を忘れた | stage1 がコマンドとして動かない | 第14章のチェックリストで確認 |
| stage2 ≠ stage3 で途方に暮れる | — | IR で diff。小さい入力から二分探索 |
| stage1 が遅すぎる | ビルドに数分かかる | 線形探索を `dict` に置換（第14章）。ただし完成後 |

---

## 7. 到達後にできること

セルフホストしたら、C 版（stage0）は**もう要らなくなります**。
言語の改良を Mython 自身で行えます。

```
新機能を実装する（Mython で書く）
  → stage2 でコンパイル → stage3（新機能を持つコンパイラ）
  → stage3 で自分をコンパイル → stage4
```

**⚠️ ただし C 版は捨てないでください。**
新機能のバグで stage が壊れたとき、stage0 から作り直せることが最後の保険になります。
これを「ブートストラップの退路」と呼びます。

### 到達後の課題（v2 のアイデア）

- アリーナ確保によるメモリ管理と高速化
- 局所的な型推論（`x = 0` から `int` を推論）
- エラー回復（複数エラーの報告）
- f-string
- ジェネリクス
- 例外機構
- LLVM C API に切り替えて `.ll` テキストを経由しない
- 自前のバックエンド（x86-64 直接出力）
